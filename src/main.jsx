import React from "react";
import ReactDOM from "react-dom/client";
import CourseSetter from "./CourseSetter.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CourseSetter />
  </React.StrictMode>
);
